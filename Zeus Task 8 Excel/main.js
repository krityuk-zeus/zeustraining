import initializeApp from './init.js';

/**
 * Initializes the application by setting up the UI and loading default data.
 * This function is the entry point for the application.
 */
initializeApp();
